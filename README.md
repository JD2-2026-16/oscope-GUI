# oscope-GUI

Junior Design II oscilloscope project GUI

## Runtime dependencies

The waveform view now uses `fastplotlib` instead of a custom `QPainter` canvas.

Install the GUI dependencies with:

```bash
python3 -m pip install -r requirements.txt
```

Then launch the app with:

```bash
python3 main.py
```
